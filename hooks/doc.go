// Package hooks holds the Claude Code plugin hooks shipped with pasture and
// their tests. The hooks themselves are shell/markdown assets under this
// directory (hooks.json, bd-prime.md, scripts/); this Go package exists only to
// carry their automated test coverage into `go test ./...`.
package hooks
