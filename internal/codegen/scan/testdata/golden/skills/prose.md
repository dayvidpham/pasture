# Title

Some intro text before any heading uses Skill(/pasture:worker) inline in a
paragraph.

## Constraints

- Then: call Skill(/pasture:supervisor) first.

> Blockquote note: remember to call Skill(/pasture:explore) before delegating.

Use `Skill(/pasture:worker)` inline code span example.
