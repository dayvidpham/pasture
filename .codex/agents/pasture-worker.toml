# Code-generated by Pasture; DO NOT EDIT.
# Standalone Codex agent profile for the "worker" protocol role.
# Runtime contract: codex/codex@0.146.0
#
# `functions` lists the native Codex functions this agent may call. Codex
# 0.146.0 exposes no skill or self-service spawn function, so it is derived
# from the pinned runtime contract's native operation bindings.
schema = "pasture.codex.agent.v1"
name = "worker"
description = "Vertical slice implementer (full production code path)"
model = "gpt-5.6-terra"
role_class = "delegated"
functions = ["request-input"]
