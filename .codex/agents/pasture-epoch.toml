# Code-generated by Pasture; DO NOT EDIT.
# Standalone Codex agent profile for the "epoch" protocol role.
# Runtime contract: codex/codex@0.153.0
#
# `functions` is derived from the pinned runtime contract's native operation
# bindings; this profile does not use the host's skill or agent-spawn
# functions, and delegation is parent-mediated by design.
schema = "pasture.codex.agent.v1"
name = "epoch"
description = "Master orchestrator for full 12-phase workflow"
model = "gpt-5.6-sol"
role_class = "primary"
functions = ["request-input"]
