# Code-generated by Pasture; DO NOT EDIT.
# Standalone Codex agent profile for the "supervisor" protocol role.
# Runtime contract: codex/codex@0.153.0
#
# `functions` lists the native Codex functions this agent may call. Codex
# 0.153.0 exposes no skill or self-service spawn function, so it is derived
# from the pinned runtime contract's native operation bindings.
schema = "pasture.codex.agent.v1"
name = "supervisor"
description = "Task coordinator, spawns workers, manages parallel execution"
model = "gpt-5.6-sol"
role_class = "primary"
functions = ["request-input"]
